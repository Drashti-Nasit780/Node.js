<!DOCTYPE html>
<html>

<head>
    <title>Edit</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: system-ui
        }

        body {
            background: linear-gradient(135deg, #dbeafe, #fef3c7);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center
        }

        .app {
            width: 480px;
            background: #fff;
            border-radius: 16px;
            padding: 22px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, .18)
        }

        .header {
            text-align: center;
            font-size: 20px;
            margin-bottom: 18px
        }

        .form {
            display: grid;
            grid-template-columns: 60px 1fr 110px 80px;
            gap: 8px;
            margin-bottom: 20px
        }

        .form input,
        .form select {
            padding: 8px;
            border-radius: 8px;
            border: 1px solid #cbd5e1;
            width: 100%
        }

        .form button {
            background: #2563eb;
            color: #fff;
            border: none;
            border-radius: 8px
        }

        .item {
            display: flex;
            justify-content: space-between;
            padding: 12px;
            border-radius: 12px;
            background: #f8fafc;
            border-left: 6px solid
        }

        .high {
            border-color: #ef4444
        }

        .medium {
            border-color: #f59e0b
        }

        .low {
            border-color: #22c55e
        }

        .no {
            font-size: 11px;
            color: #64748b
        }
    </style>
</head>

<body>
    <div class="app">
        <div class="header">Edit Task</div>

        <form class="form" action="/update-task" method="post">
            <input type="number" name="no" value="<%= task.no %>" readonly>
            <input type="text" name="tname" value="<%= task.tname %>" required>
            <select name="tprio" required>
                <option value="high" <%=task.tprio==="high" ?"selected":""%>>High</option>
                <option value="medium" <%=task.tprio==="medium" ?"selected":""%>>Medium</option>
                <option value="low" <%=task.tprio==="low" ?"selected":""%>>Low</option>
            </select>
            <button>Update</button>
        </form>

        <div class="item <%= task.tprio %>">
            <div>
                <div class="no">Task #<%= task.no %>
                </div>
                <div>
                    <%= task.tname %>
                </div>
            </div>
            <a href="/">Back</a>
        </div>

    </div>
</body>

</html>